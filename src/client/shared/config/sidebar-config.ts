// 配置
import {
    PREFIX,
} from './base-config';

const SIDEBAR_CONFIG = [{
    key: 'ui',
    title: 'UI',
    icon: 'far fa-file-alt',
    link: `${PREFIX}/index.html`,
}, {
    key: 'increase',
    title: '录入',
    icon: 'fas fa-pen-nib',
    link: `${PREFIX}/increase.html`,
}];

export default SIDEBAR_CONFIG;
