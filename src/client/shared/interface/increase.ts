// 预览所需要的mock数据
export interface MockData {
    wxml: string;
    sass: string;
    extraSass: string;
}

// 新增/修改的UI数据
export interface IncreaseData {
    name: string;
    wxml: string;
    sass: string;
    cover: string;
    category: string;
    extraSass: string;
}
